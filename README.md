# A first attempt at a basic training tool

In this project I am using fast-api to log bodyweight.
The goal for this project is to learn proper use of:

    Python
    API 
    Postgres SQL
    Docker
    CI/ID

# How to run:
```bash
docker compose up --build